# 🛍️ API.ESHOP.SITE

## About

**API.ESHOP.SITE** is a Laravel-based backend application that serves as the API layer for the **ESHOP** system.  
It provides authentication, data handling, and business logic for the frontend (`admin.eshop.site`) and other clients.

This project uses **Laravel Sanctum** for secure user authentication — supporting both SPA (cookie-based) and mobile (token-based) access.

---

## 🚀 Features

- 🔐 User authentication with **Laravel Sanctum**
- ⚙️ RESTful API architecture
- 🧩 Separate domain-based architecture:
  - `api.eshop.site` → Laravel API (backend)
  - `admin.eshop.site` → React (frontend)
- 🧠 Scalable and extensible for e-commerce development

---

## 🧰 Tech Stack

| Component | Technology |
|------------|-------------|
| Backend Framework | Laravel 11+ |
| Authentication | Laravel Sanctum |
| Database | MySQL / MariaDB |
| Server | Apache (via XAMPP for local) |
| Language | PHP 8.2+ |

---
